/** @satisfies {import("prettier").Config} */
const config = {
  arrowParens: 'always',
  bracketSameLine: true,
  bracketSpacing: true,
  printWidth: 80,
  semi: true,
  singleQuote: true,
  trailingComma: 'all',
};

module.exports = config;
